/**
 * Created by Swati Tewari on 10/20/2022
 */
public class ThirdMaxInArray {

    public void thirdMax(int[] nums) {
        int n = nums.length;


        /*for (int i : nums) {
            //sort this array in descending order
            //store elements in a hashmap for distinct
            //print the sequence first, second and third largest element in the array
            //if size of array is less than 3 then print only those elements.
            if(n < 3)
                System.out.println();
            }

        }
        return 1;*/
    }

}
